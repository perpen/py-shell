from setuptools import setup

setup(
    name='py-shell',
    version="0.1",
    description="Henri's Shell stuff",
    packages=[
        'py_shell',
    ],
    install_requires=[],
    entry_points={
        'console_scripts': [],
    }
)
